<%= form_with(model: tweet02) do |form| %>
  <% if tweet02.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet02.errors.count, "error") %> prohibited this tweet02 from being saved:</h2>

      <ul>
        <% tweet02.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
